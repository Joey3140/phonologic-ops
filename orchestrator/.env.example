# Phonologic Agentic Orchestrator - Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# APPLICATION SETTINGS
# ===========================================
ENVIRONMENT=development
DEBUG=true
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=https://ops.phonologic.cloud,http://localhost:3000

# ===========================================
# LLM CONFIGURATION (REQUIRED)
# ===========================================
# Anthropic API key for Claude agents
# You already have this in Vercel!
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Default model (Claude Sonnet 4)
DEFAULT_MODEL=claude-sonnet-4-20250514

# ===========================================
# SEARCH PROVIDER (OPTIONAL but recommended)
# ===========================================
# Serper.dev - better search results than DuckDuckGo
# Get from: https://serper.dev → Sign up → API Key
SERPER_API_KEY=

# ===========================================
# AGNO STUDIO (OPTIONAL - for debugging)
# ===========================================
# Enables visual traces of agent tool calls and reasoning
# Get from: https://agno.com → Sign up → Dashboard → API Key
AGNO_API_KEY=

# ===========================================
# CLICKUP INTEGRATION
# ===========================================
# Get from ClickUp Settings > Apps > API Token
CLICKUP_API_TOKEN=pk_your_clickup_token

# Your ClickUp workspace ID (found in URL: app.clickup.com/WORKSPACE_ID/...)
CLICKUP_WORKSPACE_ID=

# Default list ID for task creation (optional)
CLICKUP_DEFAULT_LIST_ID=

# ===========================================
# GOOGLE DRIVE/WORKSPACE
# ===========================================
# Option 1: Path to service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Option 2: Service account JSON as string (for Docker/Vercel)
# Base64 encode your service account JSON:
# cat service-account.json | base64
GOOGLE_SERVICE_ACCOUNT_JSON=

# Default folder ID for document operations
GOOGLE_DRIVE_FOLDER_ID=

# ===========================================
# SENDGRID EMAIL
# ===========================================
# Get from SendGrid Settings > API Keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key

# From email (must be verified in SendGrid)
SENDGRID_FROM_EMAIL=ops@phonologic.ca
SENDGRID_FROM_NAME=Phonologic Operations

# ===========================================
# REDIS (UPSTASH - OPTIONAL)
# ===========================================
# For session/state management across instances
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token

# ===========================================
# SECURITY
# ===========================================
# Secret key for API authentication (generate with: openssl rand -hex 32)
API_SECRET_KEY=

# ===========================================
# VERCEL PROXY (set in Vercel dashboard)
# ===========================================
# After deploying orchestrator, add this to Vercel env vars:
# ORCHESTRATOR_URL=your-orchestrator.railway.app (no https:// prefix)
